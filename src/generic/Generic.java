package src.generic;/** * 普通的泛型类 * @param <T> 这里的T只是一个标识，常见的有   T  ,E   ,K  ,V, *           T在类实例化时必须指定具体类型 */public class Generic<T extends Number> implements GenericInterface<T>{    private T key;    //    public Generic(T key){        this.key=key;    }    public T getKey() {        return key;    }    public void setKey(T key) {        this.key = key;    }    @Override    public T next() {        return null;    }    /**     * 泛型方法     * @param t     * @param <T>     * @return     */    public <T> T genericMethod(T t){        System.out.println("泛型方法测试，值为："+t);        return t;    }}